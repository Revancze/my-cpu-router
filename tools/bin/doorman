#!/bin/sh

set -u

SCRIPT_DIR=$(
    CDPATH= cd -- "$(dirname -- "$0")" &&
    pwd
)

TOOL=${0##*/}
TARGET="$SCRIPT_DIR/../$TOOL.sh"

if [ ! -f "$TARGET" ]
then
    printf 'CODELAXY ERROR: tools/%s.sh was not found.\n' "$TOOL"
    exit 1
fi

exec sh "$TARGET" "$@"