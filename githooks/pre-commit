#!/bin/sh

set -u

ROOT_DIR=$(git rev-parse --show-toplevel 2>/dev/null)

if [ -z "$ROOT_DIR" ]
then
    printf 'PRE-COMMIT ERROR: Could not find Git repository root.\n'
    exit 1
fi

cd "$ROOT_DIR" || exit 1

exec sh tools/doorman.sh
